koyeb service create \
  --app flux-ai-enhanced \
  --git https://github.com/username/flux-ai-enhanced.git \
  --git-branch main \
  --git-build-command "
    pip install --no-cache-dir --upgrade pip &&
    pip install --no-cache-dir \
      streamlit==1.25.0 \
      requests==2.28.2 \
      pillow==9.5.0 \
      psutil==5.9.0 \
      numpy==1.24.3 \
      validators==0.20.0
  " \
  --git-run-command "streamlit run app.py --server.port=\$PORT --server.address=0.0.0.0 --server.headless=true" \
  --instance-type free \
  --name flux-ai-service
